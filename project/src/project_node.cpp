#include "ros/ros.h"

int main (int argc, char **argv)
{
    ros::init(argc, argv, "project_node");
    ros::NodeHandle nh;
    // setpoint_gen_set nc = setpoint_gen_set(&nh);
    // ros::spin();
    return 0;
}